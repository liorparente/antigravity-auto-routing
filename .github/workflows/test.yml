name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install lint tooling
        run: pip install ruff mypy

      - name: Run routing-check unit and integration tests
        run: python3 skills/worker-routing/test_routing.py -v

      - name: Run production-invoker unit and integration tests
        run: python3 skills/worker-routing/test_production_invoker.py -v

      - name: Lint with ruff
        run: ruff check skills/worker-routing/routing_check.py skills/worker-routing/test_routing.py skills/worker-routing/agent_council.py skills/worker-routing/advisory_consultation.py skills/worker-routing/production_invoker.py skills/worker-routing/test_production_invoker.py skills/worker-routing/test_lmstudio.py

      - name: Type-check with mypy
        run: mypy skills/worker-routing/routing_check.py skills/worker-routing/test_routing.py skills/worker-routing/agent_council.py skills/worker-routing/advisory_consultation.py skills/worker-routing/production_invoker.py skills/worker-routing/test_production_invoker.py skills/worker-routing/test_lmstudio.py

      - name: Shellcheck installer and audit scripts
        run: shellcheck install.sh uninstall.sh skills/worker-routing/routing-audit.sh
