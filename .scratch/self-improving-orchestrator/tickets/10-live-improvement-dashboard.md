# 10 — Live improvement dashboard (follow-up build task)

**Type:** Task (build deliverable — added at the user's request during the ticket 09 grilling)
**Blocked by:** implementation of the learning journal (06) and weekly trend report (09)
**Status:** queued — enters the spec's implementation backlog; not a pre-spec decision

**What to build:** An interactive live dashboard over the learning journal and the improvement
scoreboard — the four metric families with trendlines, drill-down into individual dialogues
(rounds, verdicts, engagement units, canary results), and budget/degradation events.

**Boundary:** The weekly Markdown report (ticket 09 resolution) remains the canonical record; the
dashboard is a richer view over the same data, never a second source of truth.

**Origin:** Ticket 09 resolved "where do we see the trend" as the auto-written weekly report; the
user asked to queue the live dashboard as follow-up work rather than build it before the loop
exists.
