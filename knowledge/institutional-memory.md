# 🏛️ Institutional Memory

## 📊 Metadata
- **עדכון אחרון:** 2026-08-19
- **סה"כ תובנות:** 94

## התובנות

- `[2026-08-19] [auto-routing] [importance:5] [architecture]` - **פירוק מודולרי מלא של מנוע הדיבייט (`debate_orchestrator.py` - Spec 0008) ל-4 שכבות עצמאיות:** פירוק מנוע הדיבייט המונופוליטי לשכבות בעלות תפרי בדיקה מבודדים: מנוע מצב דטרמיניסטי טהור ללא תלויות I/O (`debate_state_machine.py`), שכבת תעבורה מבודדת לניהול תהליכי worker והתראות כשל ל-`ERRORS.md` (`debate_transport.py`), והעמקת מודולי העלה לחוזים ותמלילים (`dialogue_contracts.py`, `dialogue_transcript.py`). מודול ה-Orchestrator כווץ למעטפת דקה תוך חשיפת כינויי API היסטוריים מלאים (`run_critical_dialogue = run_advisory_consultation_debate`).
- `[2026-08-19] [auto-routing] [importance:5] [gotcha]` - **פיצול זהות מודולים בטעינת אחים לפי נתיב (`_load_sibling`) והשפעתה על Mocking:** כאשר מודולים נטענים דינמית לפי נתיב קובץ (`importlib.util.spec_from_file_location`), קשירה או שמירה של מודול מיובא ברמת הקובץ (`_production_invoker = _load_sibling(...)`) יוצרת מופע מודול נפרד מ-`sys.modules["production_invoker"]`. בדיקות יחידה שמבצעות monkeypatch על `sys.modules` החמיצו בשקט את המוק וקראו לתהליכי מערכת אמיתיים שנתקעו. הפתרון: שליפה דינמית מ-`sys.modules` בכל קריאה או הזרקת תלות מפורשת.
- `[2026-08-19] [auto-routing] [importance:4] [rule]` - **גילוי דינמי בסקריפטי התקנה והסרה: גילוי מספריית המקור (`SRC_DIR`) בלבד:** בסקריפטי uninstaller של פרוטוקולי סוכנים, גילוי קבצים להסרה חייב להתבצע מתוך ספריית המקור של הפרויקט (`SRC_DIR`) ולא על ידי glob גורף של ספריית היעד (`TARGET_DIRS`), מכיוון שספריות יעד מוסכמות (כגון `.agents/skills` או `.codex/skills`) משותפות למספר כלים ומשתמשים. glob על היעד עלול למחוק בשקט קבצים זרים.
- `[2026-08-19] [auto-routing] [importance:4] [domain]` - **קוורום מדורג עם הצבעות הימנעות (`Abstain`) בפאנל מרובה מבקרים:** הצבעת `"abstain"` הנובעת מ-timeout או שגיאת תעבורה בתהליך ה-Critic מנורמלת כהצבעה תקפה שאינה אישור (Valid Non-Approval). היא נספרת במניין הקולות הכולל אך אינה מצטרפת לאישורים, ומאפשרת חישוב קוורום מלא (unanimous, majority, qualified) ללא קריסה מיידית של כלל הדיון.
- `[2026-08-19] [auto-routing] [importance:4] [gotcha]` - **הגנת טיפוסים ב-`if TYPE_CHECKING:` במודולי עלה למניעת שגיאות Mypy/Ruff:** במודולי עלה הנטענים לפי נתיב, ייבוא טיפוסים ממודולי מעטפת חייב להישאר תחת בלוק `if TYPE_CHECKING:` קפדני. מחיקת הבלוק או הסתמכות על קשירת משתנים בזמן ריצה גורמת לשגיאות `F821 Undefined name` בלינטר ולשגיאות `[valid-type]` ב-Mypy.
- `[2026-08-18] [auto-routing] [importance:5] [architecture]` - **פירוק מודולרי מלא של מנוע הייעוץ הביקורתי (`advisory_consultation.py` - Spec 0007):** פירוק מודול `advisory_consultation.py` המונופוליטי לשלושה מודולי על ממוקדים: מרכיב פרומפטים טהור ללא תלויות (`prompt_assembler.py`), רדקטור מידע רגיש וזהויות משימה (`sensitivity_redactor.py`), ומנוע ניהול הדיון וההסכמות (`debate_orchestrator.py`). מודול הבסיס כווץ למעטפת דקה (Facade) של כ-130 שורות תוך שימור 100% תאימות לאחור (74 סמלים מיוצאים) וכל 466 הבדיקות ירוקות.
- `[2026-08-18] [auto-routing] [importance:5] [gotcha]` - **מגבלות ניתוח סטטי (AST) בלינטרים (`ruff F822`/`RUF022`) ובודקי טיפוסים (`mypy`) על מעטפות Facade דינמיות:** כאשר מודול Facade מגדיר `__all__` אך טוען סמלים באופן דינמי דרך `__getattr__`, סורקי AST כמו Ruff מייצרים שגיאת `F822`. הטיפול התקני כולל הגדרת `# noqa: F822` ומיון אלפביתי של הרשימה (`RUF022`). בנוסף, השמת שמות טיפוסים ממודולים טעונים דינמית דורשת ייבוא טיפוסים סטטי תחת `if TYPE_CHECKING:` והגנה על השמות הריצה תחת `if not TYPE_CHECKING:` כדי למנוע שגיאות טיפוס ב-Mypy.
- `[2026-08-18] [auto-routing] [importance:4] [workflow]` - **שחזור מקומי מלא של צנרת האימות של ה-CI (4-Step Verification Gate) לפני Commit:** צנרת ה-CI ב-GitHub Actions מריצה 4 שלבים קבועים: (1) כל 16 סוויטות הבדיקה של הפרויקט, (2) סריקת סגנון וייבוא עם `ruff check $PYTHON_MODULES`, (3) אימות טיפוסים עם `mypy $PYTHON_MODULES`, ו-(4) בדיקת תקינות סקריפטים עם `shellcheck`. הרצה מקומית מלאה של כל 4 השלבים בסביבה מבודדת לפני Commit מונעת רגרסיות שקטות וכישלונות בשרת הבדיקות.
- `[2026-08-17] [auto-routing] [importance:5] [architecture]` - **פירוק מודול שיחת הייעוץ המונופוליטי (Spec 0006) ל-3 מודולי-על בעלי תפרי בדיקה טהורים:** פירוק `advisory_consultation.py` משכבה אחת של 4,000+ שורות לשלושה מודולים עמוקים: חוזים ופענוח ציטוטים (`dialogue_contracts.py`), מדיניות שחיקת תקציב טהורה ללא I/O (`dialogue_degradation.py`), ורישום תמלילים וטלמטריה מאובטחת תחת נעילה (`dialogue_transcript.py`), תוך שימור מלא של תאימות ממשק אחורנית באמצעות טוען אחים דינמי.
- `[2026-08-17] [auto-routing] [importance:4] [gotcha]` - **זיהום משתפי תהליכים (Process Handler Mocks) בין סוויטות בדיקה באותו תהליך Python:** הרצת `test_routing.py` ו-`test_production_invoker.py` באותו תהליך `unittest` יחיד גרמה ל-Mocks של תהליכים מודולריים לדלוף ולפגוע בבדיקות תהליכים אמיתיות. הפרדת הריצה לתהליכי Python עצמאיים (כפי ש-CI מבצע בלולאת Bash) מבטיחה בידוד זיכרון מוחלט.
- `[2026-08-17] [auto-routing] [importance:4] [architecture]` - **קליטת שגיאות חסינה במתאמי ביקורת אסינכרוניים (`provider_adapters.CLIReviewerAdapter`):** מתאמי CLI במערך `asyncio.gather` חייבים ללכוד כל חריגת ריצה (`except Exception: # noqa: BLE001`) ולהמירה ל-Payload בטוח (`vote: "abstain"`). צמצום החריגה ל-`ValueError` בלבד מסכן את הפאנל כולו בקריסה בעת Timeout או שגיאות I/O.
- `[2026-08-17] [auto-routing] [importance:4] [gotcha]` - **החמרת כללי ארגון ייבוא (`I001` / `isort`) בריצות CI חדשות:** התקנת גרסה עדכנית של Ruff ב-CI מפעילה בדיקות סידור ייבוא (`I001`). שורת רווח מיותרת או סדר שאינו אלפביתי בקובצי בדיקות (כמו ב-`test_dialogue_transcript.py`) מפילה את ה-CI גם כאשר כל בדיקות היחידה והטיפוסים ירוקים.
- `[2026-08-17] [auto-routing] [importance:4] [pattern]` - **טוען מודולים אחים מאוחד ומוגן טיפוסים (`_load_sibling(name: str) -> Any`):** במקום לשכפל בלוקי `try/except ModuleNotFoundError -> importlib` בכל מודול עלה ופונקציית מעטפת, חילוץ פונקציית עזר יחידה `_load_sibling` עם טיפוס החזרה מפורש מונע כפילות ומאפשר טעינה ישירה של קבצים מבודדים בטסטים ללא תלות ב-`sys.path`.

- `[2026-08-17] [auto-routing] [importance:5] [architecture]` - **איחוד מנוע הרצת תהליכי סוכנים וחיסול זומבים (`production_invoker._kill_and_reap_process`):** איחוד כל מנגנוני הרצת ה-CLI האסינכרוניים ומערכי המודלים המקביליים (`invoke_worker_async`, `invoke_workers_parallel`) מאחורי מנוע מרכזי יחיד עם שומר חיסול ואיסוף תהליכים מובטח (`_kill_and_reap_process`). הפונקציה מבצעת `proc.kill()` מוגן בשילוב `await proc.wait()` הן בחריגות זמן (`asyncio.TimeoutError`) והן בחריגות תקשורת בלתי צפויות (`communicate()`), ומבטיחה אפס דליפות של תהליכי זומבי במערכת ההפעלה.
- `[2026-08-17] [auto-routing] [importance:4] [architecture]` - **חוזי שגיאה מפוצלים: כישלון זורק בודד מול כישלון מובנה באצווה אסינכרונית:** בעוד `invoke_worker` הסינכרוני זורק בכוונה `RuntimeError` בקוד יציאה שאינו 0 או ב-Timeout (כדי שדיאלוג ייעוץ בודד יזהה כישלון תור), הממשק האסינכרוני המקבילי (`invoke_worker_async`) מחזיר `WorkerExecutionResult(success=False, error=...)` ללא זריקת שגיאה. הדבר מונע מצב שבו מבקר כושל בודד בפאנל ביקורת מרובה מודלים מפיל את כל שאר ה-Coroutines הממתינים לצידו ב-`asyncio.gather`.
- `[2026-08-17] [auto-routing] [importance:4] [architecture]` - **מתאמי ביקורת כ-Thin Facades עם תפרי בדיקה מבודדים (`provider_adapters.py`):** ארגון מחדש של מחלקות המתאמים (`CLIReviewerAdapter`, `ClaudeAdapter`, `CodexAdapter`, `AgyAdapter`) לשכבת מעטפת דקה המאצילה ישירות ל-`invoke_worker_async`. המעבר ביטל כפילויות של פקודות CLI ופענוח regex, תוך שימור מלא של תאימות ממשק היסטורית וחשיפת תפר `_runner: AsyncRunner` מוזרק לבדיקות יחידה מבודדות ומהירות ללא תלות ברשת.
- `[2026-08-17] [auto-routing] [importance:4] [workflow]` - **סנכרון רשימות מודולים ובדיקות ב-CI למניעת ירוק שקט (`test.yml`):** הוספת סוויטת בדיקות חדשה למיומנות (כגון `skills/council-review/tests/test_council_review.py`) מחייבת רישום כפול ב-`.github/workflows/test.yml`: הן ב-`PYTHON_MODULES` (עבור בדיקות לינט וטיפוסים ב-Ruff ו-Mypy) והן ב-`PYTHON_TESTS` (עבור הרצה בפועל של הבדיקות). השמטה מאחת הרשימות מייצרת ריצת CI ירוקה שאינה בודקת בפועל את המודולים החדשים.

- `[2026-08-17] [auto-routing] [importance:5] [architecture]` - **צבירה אטומית ותחומה של לקחי זיכרון (Memory Lessons) בריצות מרובות (ADR 0010, כרטיס 33):** מימוש מנגנון צבירת לקחים אטומי ב-`risk_tiered_application.apply_memory_lesson`, המשלב דקדוק קנוני של פריטים מוזחים (שורת פתיחה `- `, שורות המשך מוזחות ב-2 רווחים, טיפול בטקסט Legacy), מניעת כפילויות מדויקת (Case-sensitive exact dedup), תחימת קיבולת קבועה של עד 200 לקחים (`DEFAULT_MAX_MEMORY_LESSONS = 200`) בפינוי FIFO של הלקחים הישנים ביותר, ולולאת טרנזקציה אופטימית אטומית (`_MAX_MERGE_RETRIES = 8`). המודול `learned_state.py` נשאר לחלוטין Content-Agnostic באמצעות בדיקת תנאי `expected_current` תחת ה-Store Lock.
- `[2026-08-17] [auto-routing] [importance:4] [gotcha]` - **נקודת לינאריזציה ב-Compare-And-Swap בעת דחיית מועמד (Anti-Flapping Linearization Point):** בעת ביצוע בדיקת Anti-Flapping (`reject_if_candidate_digest`) בלולאת CAS אופטימית, השוואת ה-Digest של המועמד עשויה להתבצע על מצב שכבר שונה במקביל על ידי כותב אחר. החזרת סטטוס `rejected` ללא אימות מחדש תגרום לדחיית שווא מיושנת (Stale Rejection). הפתרון: בדיקה חוזרת ש-`learned_state.read_current(root_dir).get("memory") == existing` לפני החזרת דחייה; אם המצב השתנה, הלולאה מבצעת Retry מול המצב העדכני במקום לדחות.
- `[2026-08-17] [auto-routing] [importance:4] [pattern]` - **דקדוק Round-Trip של רשימות טקסט ושימור הזחה פנימית:** בפירוק מסמך רשימה לשורות המשך מוזחות, חיתוך גורף באמצעות `lstrip()` משמיד עיצוב פנימי (כגון בלוקי קוד מוזחים). הפתרון התקני הוא חיתוך מדויק של רוחב הזחת הדקדוק בלבד (`line[len(_CONTINUATION_INDENT):]`), ושימור מלא של כל הרווחים הפנימיים שמעבר לכך.
- `[2026-08-17] [auto-routing] [importance:4] [workflow]` - **סגירת Backlog מלאה (34/34 כרטיסים) וסוויטת אימות ב-8 שכבות:** הושלמו כל 34 משימות ה-Backlog בפרויקט עם 100% מעבר (911 בדיקות ב-8 סוויטות מקומיות בתוך כ-38 שניות), אפס שגיאות ב-Ruff (`RUF022`), Mypy, Shellcheck, וסנכרון תקין בכל ה-Harnesses דרך `./install.sh .`.

- `[2026-08-15] [auto-routing] [importance:5] [architecture]` - **מנוע יישום מדורג סיכונים (RiskTieredApplication) ב-4 שכבות בטיחות (כרטיס 20):** מימוש ארכיטקטורת בטיחות מלאה עבור לולאת הלמידה האוטונומית: רמה 1 (זיכרון: החלה אוטומטית ישירה ל-`learned_state`), רמה 2 (טבלאות ניתוב: החלה אוטומטית רק לאחר מעבר מלא של שער הקבלה `acceptance_gate.evaluate_proposal`), רמה 3 (תדריכים: שמירה כהצעות ממתינות `PendingProposal` הדורשות אישור אנושי מפורש `approve_pending_proposal` תחת נעילת קבצים), ורמה 4 (פרוטוקול: חסום לחלוטין מכל נתיב כתיבה ברמת ה-Type System ומבנה הקוד via `LearnedDocument`).
- `[2026-08-15] [auto-routing] [importance:5] [gotcha]` - **מלכודת Deadlock בנעילת קבצים רקורסיבית עם POSIX `fcntl.flock`:** כאשר פונקציה ראשית (`approve_pending_proposal` / `submit_brief_proposal`) תופסת מנעול קבצים בלעדי (`fcntl.flock(..., LOCK_EX)`) וקוראת לפונקציית עזר פנימית שגם היא מנסה לפתוח ולנעול את אותו הקובץ ב-descriptor חדש, התהליך נכנס ל-Self-Deadlock ונתקע לנצח. הפתרון המחייב הוא הפרדה חדה בין פונקציות קריאה לא-נעולות (`_read_pending_proposals_unlocked`) לשימוש פנימי, לבין נקודות כניסה פומביות נעולות (`read_pending_proposals`).
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **אינווריאנט הסגירות המשולש למודולי Python חדשים (`test.yml`, `install.sh`, `uninstall.sh`):** הוספת מודול Python חדש תחת `skills/worker-routing/` דורשת עדכון מסונכרן בשלושה מקומות בו-זמנית: `.github/workflows/test.yml` (`PYTHON_MODULES` ו-`PYTHON_TESTS` עבור ruff/mypy/unittest ב-CI), `install.sh` (`MANAGED_FILES` להפצה רב-סביבתית), ו-`uninstall.sh` (`INSTALLED_FILES` להסרה נקייה). השמטה של כל אחד מהם מפילה בדיקות סגירות מערכתיות (`ManagedFileClosureTests`).
- `[2026-08-15] [auto-routing] [importance:4] [pattern]` - **טיפול ב-Idempotency ו-No-Op במאגר המצב הנלמד:** `learned_state.adopt` זורק בכוונה `ValueError` כאשר תוכן מוצע זהה לחלוטין לגרסה הקיימת. מודול ה-`risk_tiered_application` לוכד שגיאה זו וממיר אותה לסטטוס `no_op` מוצלח שמחזיר את הגרסה הקיימת, ובכך מונע קריסות שווא ומבטיח אידמפוטנטיות מלאה באימוץ חוזר.
- `[2026-08-15] [auto-routing] [importance:5] [architecture]` - **ארכיטקטורת Council Review עם ציוני ביטחון רציפים (Soft Confidence) ו-Unilateral Security Veto:** במקום הצבעה בינארית של רוב (כן/לא) בין מודלים, מועצת המודלים מבוססת על חישוב ציוני ביטחון רציפים מ-1.0- (חסימה) עד 1.0+ (אישור) עם קנס א-סימטרי (Loss Multiplier פי 1.5) על אישור קוד שבור. בנוסף, מומש שער וטו חד-צדדי (Security Veto) שבו ממצא אבטחה בודד ברמת Critical או High עוצר מיד את המועצה ל-`SECURITY_HALT` ללא תלות במשקלי ההצבעה האחרים.
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **פרדוקס מכסת ההצבעה במודלי AI מרובים (The Voting Quota Paradox לפי Banzhaf):** חלוקת משקלים בינארית נומינלית של 45% (Opus), 45% (Codex), ו-10% (Gemini) קורסת מתמטית תחת רוב פשוט (50%) למשקל שווה (33.3% לכל מודל), או תחת רוב מיוחס (60%+) ל-0% כוח הצבעה עבור מודל ה-10% (Dummy Player). הפתרון שנבחר הוא שקלול 40/40/20 בשילוב ציוני ביטחון רציפים וחסמים דינמיים $[0.05, 0.65]$.
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **קריאת `proc.kill()` ב-Asyncio משאירה תהליכי זומבי (Zombie Process Leak) ללא `await proc.wait()`:** בעת הרצת פקודות CLI של סוכנים עם `asyncio.create_subprocess_exec`, הריגת תהליך שחרג מהזמן (`TimeoutError`) משאירה את תהליך הבן כזומבי בטבלת התהליכים של מערכת ההפעלה אלא אם מבצעים `await proc.wait()`.
- `[2026-08-15] [auto-routing] [importance:5] [pattern]` - **מבקר שקורא קוד דוגם שומר אחד בכל סבב; רק סריקת מוטציה שיטתית מוצאת את כולם בבת אחת:** לולאת `/iterative-fix-review` על כרטיסים 18/26 רצה תשעה סבבים ונתקעה על פלאטו מובהק — סבבים 4 עד 7 החזירו כל אחד בדיוק פער כיסוי אמיתי אחד ועוד ערימת פרוזה תקועה, כי כל מבקר בחר שומר אחר לבדוק לעומק וקרא את השאר. סבב 8 קיבל הוראה אחרת: **למנות** כל שומר, ולידטור, ענף והמרה שהדיף הכניס, ולמטט כל אחד בנפרד — 36 מוטציות — ומצא את שלושת השורדים האחרונים במכה אחת. סבב 9 חזר על זה (22 מוטציות) ומצא אחד. ארבעה פגמים אמיתיים בשינוי הזה היו בלתי-נראים לקריאה **וירוקים ב-CI**. הכלל: כשלולאת ביקורת מציגה את חתימת הפלאטו (ממצא אמיתי אחד + פרוזה בכל סבב), להפסיק לדגום ולתקצב מעבר מוטציה שיטתי אחד — מוחקים או הופכים את שורת הייצור שהטסט מציין, מריצים, וטסט נחשב קושר רק אם הוא מאדים.
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **שומר שמשקף שומר זהה של מודול שכן הוא בלתי-נצפה, וגם `assertRaisesRegex` לא יבחין:** ל-`acceptance_gate._require_aware_now` יש תאום ב-`learning_scoreboard` שנקרא משפט אחד אחר כך, והשניים זורקים הודעה **זהה-בייט**. מחיקת המראה השאירה את כל 19 טסטי השער ירוקים — שום דבר בין השומר לבין `read_scoreboard` לא מייצר אפקט נצפה, אז המשתף זרק את אותה שגיאה בדיוק והטסט לא ידע מי מהם ירה. חשוב: התיקון אינו לשנות את נוסח ההודעה כדי שתהיה מזוהה (זה מפרק את אידיום המראה שהריפו אימץ), אלא לדחוף את המשתף לזרוק `AssertionError` אם הגיעו אליו כלל — כך הטסט בודק את **התכונה** שהמראה מספקת: סירוב בדלת הכניסה, לפני שהמשתף בכלל נקרא. הכלל: כל שומר שמשקף שומר של מודול שכן חשוד כמת עד שמוטציה מוכיחה אחרת.
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **פרמטר שאף טסט לא מעביר לו ערך לא-דיפולטי אינו נבדק כלל, גם כשהחבילה ירוקה לגמרי:** `window_days` לא הופיע פעם אחת ב-`test_acceptance_gate.py`. הסרתו מאחת משתי קריאות `read_scoreboard` השאירה הכל ירוק — ואז כל קריאה עם ערך לא-דיפולטי מתה על סירובו של `compare_scoreboards` להשוות שני חלונות שונים. הטסט המתקן לא הסתפק ב"לא נזרקה שגיאה" (זה היה תופס את המוטציה ולא מוכיח דבר על החלון עצמו): הוא דוחף את הטווח עד ש**התשובה משתנה** — ניסוי בן עשרה ימים בציון 0.98 נמצא בתוך חלון 14 ומחוץ לדיפולטי 7, ולכן אותה אצווה בדיוק נדחית ב-14 ומתקבלת ב-7. הכלל: לספור פרמטרים שאף טסט לא מעביר להם ערך לא-דיפולטי, ולבדוק כל אחד בערך שמשנה את התוצאה.
- `[2026-08-15] [auto-routing] [importance:4] [pattern]` - **אחרי תיקון אתר כתיבה אחד, לחפש את התאום שלו באותה פונקציה — טסט של מסלול מוצלח בלבד משאיר כל שדה במסלול השגיאה בלי טענה:** ענף ה-`except` ב-`evaluate_proposal` בונה `ReplayBenchmarkRecord` **משלו**, ושלושת השדות שהוא נושא (`timestamp`, `run_id`, `task_set`) שרדו מוטציה — כל אחד היה ניתן למחיקה בעוד החבילה ירוקה. הסיבה: טסט הזהות שלידו מריץ `_scripted_runner` שתמיד מצליח, ולכן לעולם לא מגיע לענף. `timestamp` היה הנזק האמיתי — השמטתו נופלת חזרה לשעון החי, הרשומה נוחתת אחרי ה-`now` המוזרק ונחתכת מהלוח על ידי `_prefix_cut`. זה היה **התאום המדויק** של הבאג שסבב קודם תיקן במסלול ההצלחה (`_wire_timestamp` בלי המרת UTC), במסלול היחיד שכל תכליתו להוכיח שכשל אינו נעדר בשקט. הכלל: תיקון באתר כתיבה אחד = grep מיידי לאותה קונסטרוקציה באותה פונקציה.
- `[2026-08-15] [auto-routing] [importance:5] [gotcha]` - **הוספת מודול לספרייה הזו נוגעת ב-ארבע רשימות נפרדות, ובדיקת הסגירות הגנה רק על שלוש — ורק בכיוון אחד:** `MANAGED_FILES` (install.sh), `INSTALLED_FILES` (uninstall.sh), `PYTHON_MODULES` ו-`PYTHON_TESTS` (CI). `ManagedFileClosureTests` בדקה רק "אם קובץ מנוהל מייבא שכן — השכן מנוהל", ולכן **מודול-עלה שאף אחד עוד לא מייבא היה בלתי-נראה לה לחלוטין** — וזה בדיוק המצב של מודול שנוחת כרטיס אחד לפני הקורא שלו. זו הפעם השלישית שאותו פער חוזר (`learning_journal`, `learning_report`, ואז `acceptance_gate`). בנפרד, `PYTHON_MODULES` נבדקה רק בכיוון רשימה→קבצים ולעולם לא קבצים→רשימה, כך שמודול שנשמט ממנה פשוט לא עובר `ruff` ו-`mypy` בשקט מוחלט. הכלל: האינווריאנט שבאמת מתקיים אינו סגירות תחת imports אלא "כל `.py` שאינו טסט חייב להופיע ברשימה", והוא חייב להיבדק **בשני הכיוונים** בכל אחת מארבע הרשימות.
- `[2026-08-15] [auto-routing] [importance:4] [gotcha]` - **הוספת משפחת רשומות חמישית שוברת ספירות כתובות בפרוזה בכל הריפו, והערכת הכרטיס לכמה מקומות היא ניחוש פתיחה ולא רשימת תיוג:** כרטיס 26 חזה שהמעבר מארבע משפחות לחמש עולה "שלושה מקומות" (docstring של המודול, `CONTEXT.md`, הספק). בפועל תוקנו כאחד-עשר לאורך תשעה סבבי ביקורת — בתוך `learning_journal.py` לבדו גם תגובת `GroundTruth`, ה-docstrings של `_validate_timestamp` ו-`TaskLabel`, תגובת `JournalRecord` וכותרת הקורא, ובנוסף `_prefix_cut`, `_is_quiet_week`, שישה אתרים ב-`test_routing.py` וחמישה אתרי "`X | None` בדיוק שלושה שדות" שהשדה `score` שבר. הרבה מהם התגלו **בסבב שאחרי** התיקון שכתב אותם. הכלל: ספירה כתובה בפרוזה היא התחייבות תחזוקה — לפני שמכריזים שהמעבר הושלם, לעשות grep על המילים המונות ועל מילות המיצוי (`only`, `every`, `exactly`, `never`, `nothing else`), ולא להסתמך על רשימת המקומות שהכרטיס ניחש.

- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **סבב אימות בדיאלוג Planner-Critic יכול לגלות שהתיקון להשגה הקודמת בעצמו הכניס פגם חדש — לא רק ניסוח קוסמטי:** קרה פעמיים באותה שיחה, בשני מנגנונים שונים. בתכנון כרטיס 17: סבב הביקורת השני (על תיקוני הסבב הראשון) לא הסתפק ב"תוקן" — הוא בדק כל תיקון מחדש מול המקור, ומצא שלוש הכרעות שגויות חדשות שהתיקונים עצמם הכניסו (מנגנון צירוף family↔metric לא מפורט, פורמט חותמת-זמן לא מוגדר, טענת "אין מקום שני לעדכן" שהתבררה כשגויה). בביקורת הקוד שלאחר הביצוע: תיקון לבדיקת `NaN` (כרטיס 16) הכניס בעצמו תגובה עם טענת-כיסוי לא מגובה, שנתפסה רק בסבב ביקורת נוסף. הכלל: כשמריצים לולאת תיקון-ואז-ביקורת, סבב האימות חייב לבדוק את **המנגנון** של כל תיקון מול המקור מחדש, לא רק לאשר ש"ההשגה טופלה" — אחרת התיקון עצמו הופך למקור התקלה הבאה.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **`agy -p --model <id>` מתעלם בשקט מהדגל `--model` במצב הדפסה, ותמיד עונה כמודל ברירת המחדל הקבוע של סשן ה-IDE:** שתי שאילתות בדיקה נפרדות ("תגיד לי איזה מודל אתה") ענו `Gemini 3.5 Flash (High)` ללא קשר לדגל `--model gpt-oss-120b-medium` / `--model gemini-3.1-pro-high` שהועבר, וקריאת המשימה המלאה הראשונה גם החזירה תשובה לשאלה שמעולם לא נשאלה. `agy models` שמציג מזהה מודל אינו הוכחה שהוא באמת ירוץ תחת `-p`. הכלל: לפני שסומכים על ניתוב `agy` לכל משימה אמיתית (במיוחד תפקיד Critic בדיאלוג), לבדוק עם שאילתת "תגיד לי איזה מודל אתה" זולה — קוד יציאה 0 ותשובה שנראית תקינה אינם מספיקים.
- `[2026-08-14] [auto-routing] [importance:4] [pattern]` - **ביקורת `/code-review` שמוגבלת לדיף של כרטיס בודד לא יכולה לתפוס פערים שיושבים בדיוק על הגבול שבין כרטיסים:** כרטיס 17 הוסיף `learning_report.py` בלי לחווט אותו ל-`install.sh`/`uninstall.sh`, בדיוק כפי שכרטיס 16 חיווט את `learning_scoreboard.py` המקביל לו. בדיקת הסגירות הקיימת (`ManagedFileClosureTests`) לא יכלה לתפוס את זה — היא בודקת רק שאם קובץ מנוהל מייבא שכן, השכן גם מנוהל, ושום דבר ב-`MANAGED_FILES` לא מייבא את `learning_report`. רק מעבר ביקורת רחב יותר (`origin/main` מול `HEAD` מקומי, חוצה שלושה כרטיסים בבת אחת) חשף את הפער. הכלל: ביקורת שמוגבלת לתחום כרטיס בודד תמיד תפספס פערי-חיווט בין-כרטיסיים; שווה להריץ מעת לעת מעבר רחב יותר, לא רק בדיקה פר-כרטיס.
- `[2026-08-14] [auto-routing] [importance:5] [gotcha]` - **תיקון בדיקה כדי שתבדוק בפועל את מה שהיא טוענת יכול לחשוף באג אמיתי ורדום במודול אחר לגמרי, כתוצר לוואי טהור — והדחף לכתוב "מתועד בנפרד" בתגובת קוד אינו תיעוד אמיתי:** תיקון לבדיקה על `_classify_change` (שהייתה כפולה-בפועל לבדיקה אחרת, כי ה-`assertRaises` שלה נכשל לפני שהקוד הנבדק אי-פעם רץ) חשף — כתוצר לוואי, לא כמטרה — ש-`_classify_change` מסווג `NaN` כ"השתפר" תחת `lower_is_better`. לא ניתן לניצול היום (הבנאי הציבורי חוסם `NaN` מראש), אבל התיקון הראשון לבדיקה כתב בתגובה "מתועד בנפרד" בלי שום ארטיפקט אמיתי מאחורי הטענה — בדיוק התבנית שכבר מתועדת בריפו הזה ("הודעת commit שסחפה טענת-אימות לא קיימת"), הפעם בתגובת קוד ולא בהודעת commit. נתפס בסבב ביקורת נוסף. תוקן על ידי תיוק טיקט אמיתי (28) שהתגובה מפנה אליו בפועל. הכלל: "מתועד בנפרד" בתוך קוד או תגובה אינו תיעוד — התיקון הוא לתייק טיקט/רשומה אמיתיים לפני שכותבים את המשפט הזה, לא אחרי שביקורת תופסת את הפער.
- `[2026-08-14] [auto-routing] [importance:3] [pattern]` - **`git add -N <קובץ-חדש>` (intent-to-add) נדרש לפני ש-`git diff HEAD -- <נתיבים>` יציג את תוכן קובץ חדש שלא במעקב:** בהכנת דיף לביקורת-קוד לפני commit, קבצים חדשים (`??` ב-`git status`) לא מופיעים כלל ב-`git diff HEAD`, גם אם `--` מציין את הנתיב שלהם במפורש — הפקודה פשוט מדלגת עליהם בשקט. `git add -N` מסמן אותם כ"כוונה להוסיף" בלי לשנות את תוכנם בפועל, וממנו והלאה הם מופיעים בדיף כתוספת מלאה. הכלל: לפני שמכינים דיף לביקורת שכולל קבצים חדשים שטרם הועלו ל-git, לוודא `git add -N` עליהם קודם.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **שני סשנים של Claude Code יכולים לחלוק עץ עבודה אחד ולערוך במקביל את אותם קבצי תיעוד משותפים:** `ERRORS.md` ו-`institutional-memory.md` הצטברו במהלך השיחה תוכן שלא נכתב על ידי הסשן הזה — עשר רשומות זיכרון ושלוש רשומות שגיאה שמתעדות את סגירת כרטיס 16, שקרתה *לפני* תחילת השיחה הזו. `git status` בלבד לא מבחין בין "אני כתבתי את זה" ל"סשן אחר כתב את זה." הכלל: לפני commit על קובץ תיעוד משותף לבקשת המשתמש, לקרוא את כל הדיף (לא רק את התוספת האחרונה) כדי לוודא שאין קטיעה באמצע רשומה (סימן לכתיבה פעילה של סשן אחר), ולכתוב הודעת commit כנה שלא לוקחת קרדיט על תוכן שהסשן הזה לא כתב בפועל.
- `[2026-08-14] [auto-routing] [importance:3] [pattern]` - **כשפרוטוקול הניתוב לא מגדיר מנגנון מפורש ל"האדם אישר את זה" מחוץ למנגנון הדיאלוג האוטומטי שלו, רצף פקודות-לוכסן שהמשתמש עצמו הפעיל יכול לשמש כתחליף סביר — אם אומרים את זה במפורש:** רישום ground truth ל-`review=approved` נעשה לא מתוך "כן, מאושר" מילולי, אלא מתוך רצף `proceed` → `/iterative-fix-review` → `/implement` (שהוראתו המפורשת היא "commit את העבודה שלך") — רצף שמניח קבלה של תוצאת הביקורת בלי לאמר זאת בפירוש. הכלל: כשמסיקים אישור אנושי מרצף פעולות ולא מהצהרה מילולית, לרשום את ההיסק הזה במפורש ברשומת הסגירה (למשל בהודעת ה-commit) — לא להתייחס אליו בשקט כאילו הוא זהה למסלול האוטומטי המתועד בפרוטוקול.

- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **שני קבצים שמתעדים את הסיבה מאחורי החלטות כרטיס 16 יושבים כרגע בסיכון אובדן, בדיוק לפי התבנית שכבר תועדה:** `implementation_plan.md` (בשורש הריפו, 1694 שורות — התוכנית שעברה קונצנזוס Planner-Critic בן ארבעה סבבים) אינו במעקב Git כלל. [`.scratch/planning_debate.md`](file:///Users/liorparente/Projects/auto-routing/.scratch/planning_debate.md) (תמליל הביקורת המלא של Codex Sol, כולל שני "VERDICT: REVISE" ו-"VERDICT: APPROVED") יושב ישירות תחת `.scratch/`, לא תחת `.scratch/<name>/issues/**` — כך שהוא נופל תחת חוק ה-`.gitignore` הגורף (`.scratch/**`) ואינו נתפס באף אחד מארבעת דפוסי ההחרגה (`issues/**`, `tickets/**`, `map.md`, `spec.md`). זו בדיוק התבנית שתועדה קודם היום ("תיקיית scratch תצבור דברים שאינם scratch, וכלל התעלמות גורף ימחק אותם בשקט") — רק שהפעם היא נתפסה *לפני* אובדן בפועל, לא אחריו. הכלל: קובץ תוכנית/דיבייט שנוצר תוך כדי סשן הוא בדיוק מהסוג שהמבחן חל עליו — "האם אובדנו יעלה החלטה" — ולא משנה שהוא "רק" תוצר-ביניים של הלולאה.
- `[2026-08-14] [auto-routing] [importance:5] [pattern]` - **לולאת קונצנזוס Planner-Critic יעילה גם לתכנון עצמו, לא רק לביקורת קוד אחרי מעשה:** לפני שנכתבה שורת קוד אחת עבור כרטיס 16, הורצו ארבעה סבבים בין Planner (Claude Opus 5) ל-Critic (Codex Sol) על `implementation_plan.md` עצמו — עד ל-`VERDICT: APPROVED` מפורש. בסבב 2 ה-Planner לא נכנע לכל השגה: הוא **דחה** את מחצית השגה 4 (צירוף עלות ברמת run_id) בראיות מצוטטות מתוך התיעוד עצמו (`WorkerExecutionRecord`'s docstring), וה-Critic חזר בסבב 3 והודה שהדחייה נכונה. שתיקה או "נראה טוב" לא נספרו כהסכמה באף שלב — בדיוק העיקרון שהריפו הזה כבר אימץ לביקורת קוד. הכלל: למשימות ברמת Complex, הרץ קונצנזוס Planner-Critic *לפני* ביצוע, לא רק אחריו — התוכנית שיוצאת מהתהליך תפסה שגיאות (מפתח-מיזוג שגוי, מדד "הושלמה" שסופר תוכניות שלא יושמו כלל) שסבב תכנון בודד לא היה תופס.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **הנחיה מתקנת של האורקסטרטור ל-worker עלולה לזרוע בעצמה את הבאג הבא:** בסבב הביקורת הראשון על קורא-היומן החדש, נאמר לעובד "אל תסמן אי-קריאוּת עבור מפתח שיש לו ברירת מחדל" — הנחיה שנכונה כשמדברים על נוחות בנאי (צד כתיבה), אבל שגויה כשמיישמים אותה על חוזה-החוט (צד קריאה): שדה `timestamp` שנעדר משורה ביומן קיבל, בעקבות ההנחיה, ברירת מחדל שקטה של *זמן הקריאה הנוכחי* — כלומר בדיית עדות היסטורית. נתפס רק בסבב הביקורת הבא, כשהמבקר שאל "מה חייב להישבר כדי שזה ייכשל". הכלל: לפני שמכלילים הנחיה מתקנת מ-"אל תעשה X" ל-worker, לבדוק אם X בכלל מבלבל בין שני דברים נפרדים (נוחות בנייה מול חוזה קריאה) — אחרת ההנחיה עצמה יוצרת את החור.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **הממצא האחרון בלולאת ביקורת יכול לחיות בתשתית משותפת שקדמה לכרטיס, לא ב-diff עצמו — וזה משנה את פרופיל הסיכון:** אחרי חמישה סבבי תיקון על קורא-היומן החדש, הסבב החמישי מצא ש-`_validate_timestamp` (משותף לכל ארבע מחלקות הרשומה, קיים הרבה לפני כרטיס 16) בדק רק **תבנית** של חותמת-זמן ולא תקפות-לוח-שנה — כך שאפשר היה לבנות `ComplianceRecord(timestamp="2026-99-99T99:99:99Z")` בהצלחה, לכתוב אותו ליומן בלי שגיאה, ורק הקורא **החדש** (המחמיר ממנו) גילה בקריאה שהוא לא תאריך אמיתי ומחק אותו בשקט. אומת ישירות (בנייה, כתיבה, קריאה-חוזרת) לפני ואחרי התיקון. הכלל: כשממצא נוגע בקוד משותף שקדם לשינוי הנוכחי, לא רק בקבצים החדשים — פרופיל הסיכון שונה (רגרסיה כלל-ריפואית, לא מקומית), ומחייב הרצת הסוויטה המלאה (לא רק שני הקבצים שנערכו) לפני שממשיכים.
- `[2026-08-14] [auto-routing] [importance:3] [architecture]` - **החלטת הסֵם של כרטיס 16 שלב 1: הקורא של היומן יושב במודול שבבעלות הסכמה, לא במודול הצרכן:** `read_journal`/`JournalRead`/`parse_wire_timestamp` נוספו ל-[`learning_journal.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/learning_journal.py) (המודול שכבר מגדיר את כל ארבע מחלקות הרשומה) ולא ל-`learning_scoreboard.py` החדש שצורך אותו — הוכרע במבחן-המחיקה: מחיקת הקורא לא מבטלת את המורכבות אלא מעבירה אותה לשלושה צרכנים עתידיים (כרטיסים 17, 18, 22), שכל אחד מהם היה צריך לממש מחדש את פענוח ה-`kind` ואת שיטוח ה-`TaskLabel` בעצמו. קומיט `0c8ed7c`.
- `[2026-08-14] [auto-routing] [importance:5] [gotcha]` - **הודעת commit שכתב האורקסטרטור עצמו סחפה טענת-אימות שלא הייתה קיימת בקוד המחויב:** בקומיט `dabfc5f` (תיקון בדיקת "אין שעון" של כרטיס 16) נכתב "False-positive resistance checked directly against ten hand-written cases" ו-"two new tests confirm both the catch... and the non-catch (`journal.now()`...)" — אבל הקובץ נשא בפועל **שני** טסטים בלבד, שניהם catch-בלבד; עשרת המקרים נבדקו רק בסקריפט אד-הוק פרטי של האורקסטרטור, מעולם לא הפכו לטסטים מחויבים. `grep -n "journal.now\|today()\|hand-written"` על הקובץ המחויב החזיר ריק — זה מה שחשף את הפער בסבב הביקורת השני, לא קריאה חוזרת של הקוד. תוקן ב-`8cab197`, שהוסיף בפועל את ארבעת הטסטים שהוצהרו. הרחבה קריטית לתובנה "משפט בטוח בלי מראה-מקום" (2026-08-13, למטה): עד כה התבנית תועדה כשהמקור היה עובד או סשן-עמית; הפעם המקור היה האורקסטרטור עצמו, בהודעת commit שהוא כותב תחת שמו. הכלל: כל טענת-כיסוי בהודעת commit ("נבדק מול X מקרים", "טסטים מאמתים Y") חייבת להצביע על שם-טסט קיים בקובץ, לא על בדיקה שבוצעה מחוץ לסוויטה — אחרת ההודעה עצמה היא בדיוק סוג הטענה שהריפו הזה מלמד לחשוד בה.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **שדה `**Status:**` בקובץ טיקט יכול לפגר אחרי מה שכבר נחת ב-Git, לא רק אחרי מה שעוד לא נחת:** [`16-scoreboard.md`](file:///Users/liorparente/Projects/auto-routing/.scratch/routing-backlog/issues/16-scoreboard.md) הצהיר `ready-for-agent` גם אחרי ששלב 1 (קורא היומן) כבר נחת ב-`main` (`0c8ed7c`, `5a26606`) — כלומר תשובה שנשענת רק על שורת הסטטוס טעתה בכיוון ה**שמרני**: הציגה עבודה שכבר בוצעה כלא-מתחילה, לא רק את הכיוון האופטימי הרגיל (מוצג כגמור בזמן שעוד פתוח). נתפס כי המשתמש ביקש בדיקה חוזרת מפורשת, לא כי בדיקה ראשונית חשפה זאת מעצמה. הכלל: לפני שעונים "מה פתוח הבא" מקובץ טיקט, לחפש ישירות אחר שמות המודולים/הפונקציות שהטיקט אמור להוליד (`grep`/`git log`) — לא להסתפק בשורת הסטטוס שלו לבדה.
- `[2026-08-14] [auto-routing] [importance:4] [gotcha]` - **פרוטוקול רישום-אמת ליומן הלמידה (Spec 0004 Ticket 25) לא היה מיושם בפועל עד עכשיו:** ניסיון לרשום את תוצאות כרטיס 16 (`learning_outcomes.record_test_result`/`record_review_verdict`) חשף ש-`.ralph/learning_journal.jsonl` לא היה קיים בכלל על הדיסק — כלומר גם כרטיסים 24 ו-25, שבנו את מנגנון `learning_outcomes.py` הזה במלואו, מעולם לא קראו בפועל לנקודות הכניסה שלהם, למרות ששניהם מסומנים `done`. `.ralph/` מוחרג לגמרי מ-git, כך שגם סשן שכן היה קורא לפונקציות האלה לא היה משאיר עדות שסשן אחר יכול לגלות בלי להריץ את הבדיקה בעצמו. הכלל: צעד פרוטוקול שהעדות שלו יושבת בקובץ מקומי-בכוונה (מוחרג-git) לא ניתן לאימות מקריאת הריפו — רק מניסיון להפעיל אותו בפועל. לפני שמניחים שכרטיס "גמור" ביצע כל צעד סגירה שהפרוטוקול דורש ממנו, יש לבדוק אם התוצר שהצעד אמור היה להשאיר קיים בכלל.
- `[2026-08-14] [auto-routing] [importance:3] [architecture]` - **`.scratch/**` מוחרג לגמרי מ-git, פרט לארבעה דפוסי-נגד מפורשים בלבד:** `.gitignore` שורות 22-27 קובעות `.scratch/**` ואז מחריגות-נגד רק את התיקיות עצמן, `.scratch/*/issues/**`, `.scratch/*/tickets/**`, `.scratch/*/map.md` ו-`.scratch/*/spec.md`. `.scratch/plans/` **אינו** ברשימה — כלומר `implementation_plan.md` שהועבר אליו בסגירת כרטיס 16 (בעקבות התקדים של כרטיס 24, `.scratch/plans/24-dialogue-quality-writer.md`) הוא קובץ מקומי-בכוונה, לא "שנשכח מלהוסיף לגיט". `git mv`/`git add` עליו נכשל בשקט (`git status` פשוט לא מציג אותו), לא בשגיאה רועשת — כמעט הוביל לניסיון הוספה כפויה שגוי. הכלל: לפני שמניחים ש-`.scratch/<x>/<file>` "צריך" מעקב גרסאות, יש לבדוק את דפוסי ההחרגה במפורש — לא כל מה שיש לו תבנית-כרטיס נופל תחתם.
- `[2026-08-14] [auto-routing] [importance:3] [pattern]` - **"הופסק על ידי המשתמש" מ-sub-agent לא תמיד אומר שהמשתמש לחץ על משהו במכוון:** שני sub-agents של `/code-review` חזרו עם "was stopped by the user and won't be resumed" בלי פעולה נראית לעין בשיחה. הכלי עצמו מנחה "only launch a new agent if the user explicitly asks" — אבל הנחיית `/goal` סטנדינג שכבר ניתנה קודם בשיחה נספרה כבקשה מפורשת מספקת לשיגור מחדש, בלי לעצור ולשאול שוב. הכלל: כשכלי-רקע מדווח שמשהו "הופסק על ידי המשתמש" בלי שראית פעולה כזו, אין להמציא תוצאה ואין לשגר מחדש בשקט — אבל הנחיה עומדת שכבר ניתנה במפורש (כמו `/goal`) יכולה לשמש כ"בקשה מפורשת" בלי לעצור שוב.

- `[2026-08-13] [auto-routing] [importance:4] [workflow]` - **התכנסות של שני צירי ביקורת בלתי-תלויים על אותו ממצא, מזוויות שונות, היא האות החזק ביותר שיש — ואם התיקון דורש הכרעת מידול, יש לעצור ולשאול:** בסבב תיקונים על כרטיס עשרים וחמש, ציר Standards וציר Spec פסלו את אותו מיפוי (`stalemate → rejected`) בנפרד לגמרי, כל אחד מהכיוון שלו — האחד תפס שהוא מתאר הזדמנויות שלא מייצרות תוכנית כלל, השני תפס שהוא מבלבל בין אישור מבקר וירטואלי לאישור אנושי. ההתכנסות משני כיוונים בלתי תלויים היא אות חזק בהרבה מממצא בודד. זוהתה גם ההבחנה בין תקלה מכנית (אפשר לתקן ולנתב לעובד) לבין שאלת מידול-תחום (מה בכלל אומר "תוכנית התקבלה") — ובמקרה השני נעשה שימוש בכלי שאלת-משתמש במקום החלטה חד-צדדית. הכלל: מחלוקת בין שני צירים אינה רק אות לתקלה בקוד — היא אות שכדאי לבדוק אם ההכרעה הנדרשת היא בכלל שלך לקבל.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **דיווח "הכול ירוק" מעובד תקף רק אם הכלי שהריץ אותו עובד בעצמו:** בסבב תיקונים על כרטיס עשרים וחמש, עובד אחד דיווח ש-`ruff` ו-`mypy` עברו נקי — אבל הריץ אותם דרך ה-`.venv` השבור של הריפו (הועבר בין תיקיות וה-`.venv` לא שוחזר), כך שהדיווח לא נשא שום ראיה על הקוד עצמו. נתפס רק כי הרצתי מחדש בעצמי דרך `pipx run`. הרחבה של תובנת 2026-08-11 "דיווח עובד על קבצים שאינו בבעלותו אינו אימות": הכלל חל גם על דיווחי **אימות** — "עברתי בדיקה" אינו ראיה בלי לדעת שהבדיקה עצמה רצה על כלים תקינים. הכלל: להריץ את שער האימות בעצמך תמיד, גם כשעובד מצהיר שכבר עשה זאת.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **התבנית ש"פתחה" את כרטיס עשרים וחמש חזרה עוד פעמיים בתוך התיקון שלו עצמו:** אחרי שכרטיס עשרים וחמש נסגר (קומיט `533360f`), התבנית שהולידה אותו — assertion שמתקיים בלי שהנתיב הנקרא על שמו רץ בפועל — הופיעה עוד פעמיים בתוך סבב הביקורת-ותיקון החוזר: בדיקת כשל-כתיבה שרכיב אחר (כרטיס עשרים וארבע) נכשל בו ראשון, כך שהצלחת הבדיקה לא הוכיחה כלום על הרכיב הנבדק; וקורלציית `run_id` שנבדקה דרך איחוד קבוצות שמתכווץ לאיבר אחד גם כשאחת הקבוצות ריקה לגמרי. שלוש הפעמים הן צורות שונות של אותה תקלה יחידה — assertion שיכול להתקיים בלי שהנתיב שהוא טוען לבדוק אי פעם רץ. שתיים מהן נתפסו רק בביקורת, לא על ידי הבדיקות עצמן. זו התבנית השולטת בריפו הזה. הכלל: לכל assertion חדש או משתנה — לשאול "מה חייב להישבר כדי שזה ייכשל?", ולבדוק אם משהו אחר כבר מספק את התשובה מבלי לגעת בנתיב הנבדק. פירוט מלא ב-`ERRORS.md`.
- `[2026-08-13] [auto-routing] [importance:3] [workflow]` - **זמינות `codex` היא עובדה משתנה בזמן, לא הערת זיכרון קבועה:** רשומת זיכרון קודמת קבעה ש-`codex` מוצה עד 2026-08-18. בדיקת בדיקה חיה (`PROBE_OK`) הראתה שהוא זמין לגמרי כבר ב-2026-08-13, ושש ריצות סקירה ב-`model_reasoning_effort="high"` רצו בלי קיר. הכלל: לפני שמסתמכים על הערת "מצב מוכר" בזיכרון, יש לבדוק חי — הערה כזאת היא צילום רגע, לא אמת קבועה.
- `[2026-08-13] [auto-routing] [importance:3] [workflow]` - **סקירת קוד עם `codex review` צריכה `sandbox_mode="read-only"`, והרעש שהיא מייצרת הוא צפוי:** מבקר עם הרשאת כתיבה עלול "לתקן" תוך כדי סקירה ואז לדווח על קוד שכבר לא קיים — לכן `read-only` הוא הבחירה הנכונה לכל ביקורת. המחיר: כשהמבקר מנסה להריץ את חבילת הבדיקות בעצמו הוא נכשל עם `No usable temporary directory found`, כי הסנדבוקס חוסם כתיבה גם לתיקיית הזמניים. זו לא עדות לכשל אמיתי — היא תוצר לוואי של מצב ה-`sandbox`, וההרצה העצמאית מחוץ לסנדבוקס היא הקובעת.

- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **רכיב יכול לעבור כל שער איכות בלי שאיש בעולם קורא לו — נסגר ב-`533360f`:** כרטיס 14 הנחית את `learning_outcomes.py` — נקודות כניסה מאומתות, מכוסות בבדיקות, רשומות ב-`install.sh` וב-CI. `grep -c learning_outcomes` מחזיר **אפס** גם ב-`advisory_consultation.py` וגם ב-`routing_check.py`; שני האזכורים ב-`production_invoker.py` הם בתוך docstrings ואין בו `import` כלל. משפחת ה-outcome — זו שעונה על "האם צדקנו" — היא ספרייה בלי יצרן, ולכן היומן מתעד כל החלטה ואף תוצאה. שים לב שאף שער לא יכול היה לתפוס זאת: קריטריון הקבלה ביקש "נקודת כניסה ציבורית שרושמת תוצאה" והתקבלה כזו; הבדיקות מריצות את נקודת הכניסה **ישירות** ולכן ירוקות; סקירה רואה מודול בנוי היטב; ו-`install.sh`/CI מוודאים שהוא מותקן ונבדק — כולם מאמתים שהרכיב **קיים ותקין**, אף אחד לא שואל אם הוא **מופעל**. הכלל: לרכיב שכל תכליתו להיקרא, קריטריון קבלה אחד חייב לנקוב בקורא בשמו, ובדיקה אחת חייבת להגיע אליו דרך מסלול הקורא ולא דרך נקודת הכניסה. **נסגר** — `plan` קיבל נתיב קוד, שלוש האמיתות האחרות קיבלו צעד מתועד ב-`protocol.md`. ראה את התובנה למעלה על התבנית שחזרה בתוך התיקון עצמו.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **משפט בהווה במפרט יכול להיות הנחה שלבושה כתיאור:** [`docs/specs/0004-learning-loop.md`](file:///Users/liorparente/Projects/auto-routing/docs/specs/0004-learning-loop.md) קבע שרישומי outcome "נכתבים כשכל אמת נודעת" — ניסוח שנקרא כתיאור התנהגות קיימת, ובפועל היה כוונה שאיש לא הוקצה לה: כרטיס 14 בנה את נקודות הכניסה ואף כרטיס לא הקצה את הקוראים. הסכנה ייחודית לתפרים בין רכיבים: כשמפרט מתאר מה קורה **בגבול** בין שני חלקים, קל לכתוב את המשפט בהווה ולהניח שהצד השני יטפל. הכלל: כל משפט מפרט שמתאר התנהגות בתפר חייב להיבדק מול "איזה כרטיס מחזיק בכל צד" — ניסוח בהווה אינו בעלות. תוקן ב-`889b719`, שם המשפט אומר עכשיו מי מחזיק בכל חצי. אחות לתובנה "מסמך משימה שמתאר עבודה עתידית כעובדה קיימת" למטה.
- `[2026-08-13] [auto-routing] [importance:4] [workflow]` - **תיקייה בשם "scratch" תצבור דברים שאינם scratch, וכלל התעלמות גורף ימחק אותם בשקט:** שלושים וארבעה מתוך שלושים וחמישה כרטיסים בפרויקט הזה חיו מחוץ לניהול הגרסאות לכל אורך חייו, מוסתרים מאחורי `.scratch/` ב-`.gitignore`; היחיד שכן נוסף (`07-production-worker-invoker.md`) נוסף רק כי קדם לכלל. הכרטיסים, המפות והמפרטים בטיוטה הם התיעוד היחיד של **למה** הקוד נראה כפי שהוא — הקוד מתעד את ה"מה". הסיכון הפך מוחשי באותו יום: worktree מוזג-במלואו לצד אותם קבצים נמחק באמצע סשן. תוקן ב-`b266d88` (שישים קבצים; מנגנון ההחרגה-נגד תועד ב-`global-memory.md`). הכלל: המבחן אינו "האם זה זמני" אלא "האם אובדן הקובץ הזה יעלה לי החלטה". אם כן — הוא לא scratch, ולא משנה באיזו תיקייה הוא יושב.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **דחיפה אינה מוגבלת לקומיטים שלך — ולכן היא חוצה גבול הרשאות של סשן אחר:** דחפתי את `ec20c87` באישור המשתמש שלי, והדחיפה פרסמה גם את `ad66416` — קומיט של סשן עמית שהוחזק **במכוון** מקומית, כי דחיפה בסשן שלו מחייבת אישור מפורש של המשתמש שלו, והוא עוד המתין לתשובה. `git push` אינו מכיר מושג של "הקומיטים שלי": הוא מקדם מצביע, וזה מפרסם את **כל שרשרת האבות**. אומת: `git branch -r --contains ad66416` מחזיר `origin/main`, ו-`ad66416` הוא אב-קדמון של `ec20c87`. שים לב מדוע הזהירות שכבר תיקנתי לא תפסה זאת: `git add <path>` מפורש ו-`git status` לפני הקימוט מסתכלים על **עץ העבודה**; דחיפה פועלת על **טווח קומיטים**, אובייקט אחר לגמרי. אפילו ראיתי את `ad66416` כ-HEAD וקראתי זאת כ"העמית קימט" במקום כ"הקומיט שלו יושב תחתי ויישלח איתי". הכלל: לפני כל דחיפה — `git log origin/main..main`, ומנה בשם כל קומיט שאינו שלך; אם יש כזה, שאל את מחברו. מסקנה נלווית: בעץ משותף הגייט היחיד האמיתי על פרסום הוא **לא לקמט עדיין** — קומיט מקומי הוא כבר מצב משותף. אישור שניתן בסשן אחד אינו חל על עבודתו של סשן אחר שיושבת באותה היסטוריה.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **אימות חלקי מייצר ביטחון לפי מה שנבדק, לא לפי מה שנותר:** סשן עמית מסר חמש טענות; אימתתי ארבע, וה**חמישית — היחידה שדילגתי עליה — הייתה השגויה**. הדפוס אינו מקרי: ארבע הטענות שבדקתי היו כולן עובדות מכניות (`grep` מחזיר `N`, קובץ מגדיר מחלקה), והחמישית הייתה טענה **פרשנית** ("טיקט 24 היה באחריות ספק 0003"). אימות נוטה מטבעו אל הבדיק ומדלג על הפרשני — וזה בדיוק הפוך, כי הפרשנות היא מקום מגורי הטעות. גרוע מכך, ארבעת האימותים שהצליחו העניקו לטענה החמישית ביטחון שלא נבדק כלל, ובעקבותיה דיווחתי למשתמש שסיכום ספק 0003 שגוי — בעוד `docs/specs/0003-critical-dialogue.md` שורה 209 מתנער במפורש: *"The LearningJournal, learner, scoreboard, and weekly report — spec 0004"*. חידוד שהעמית הוסיף נגד הכשל של עצמו, ושאני אישרתי מהצד שלי: **עלות אינה המבחן.** הטענה שהוא לא אימת הייתה זולה — שלושים שניות של קריאה — והוא דילג עליה בכל זאת; אני אימתתי אותה ב-`grep` יחיד. לכן "אמת קודם את היקר" נחוץ אך לא מספיק. המבחן האמיתי הוא **רגע הכתיבה**: אם משפט טוען עובדה על הקוד ואינו יכול לשאת `file:line`, הוא לא אומת — ולא משנה כמה זול היה לאמת אותו. שני הכשלים האמיתיים של היום נשאו בדיוק את החתימה הזאת: "אף אחד מהם לא קיים בקוד הזה" שלי, וטענת בעלות על סעיף מפרט שלא נפתח שלו. הכלל המלא: סווג כל טענה כ"עובדה" או כ"מסקנה" לפני הבדיקה; אל תדווח על שגיאה של מישהו אחר על סמך טענה שלא בדקת; ואל תכתוב משפט בטוח בלי מראה-מקום מאחוריו.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **טיקט חוצה-מפרטים חייב להיבדק מול ה-Out of Scope של הצד השני:** טיקט 24 (הכותב ליומן איכות-הדיאלוג) הוקצה לספק 0003 לביצוע, בעוד ספק 0003 כבר הכריז בשורה 209 שה-LearningJournal שייך לספק 0004. התוצאה: הטיקט היה **חסר-בעלים מיום הגשתו**, לא נטוש בסגירה. אף בדיקה ואף סקירה לא יכלו לתפוס זאת, כי שני הצדדים קוראים את אותה שורת סטטוס כבעיה של מישהו אחר, ושני הסיכומים נראים מדויקים. הכלל: הקצאת טיקט חוצה-מפרטים נקבעת מול סעיף ה-Out of Scope של המפרט המקבל, לא מול השאלה מי לדעת המחבר במיקום הטוב ביותר לבצע. הבעלות הוחזרה לספק 0004 ב-`ad66416`.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **אימות "כותב יחיד" פג תוקף מיד, ו-`git add -A` הופך פקיעה כזאת לנזק:** אימתתי כותב-יחיד ב-17:20 (סשן העמית נסגר, העץ נקי), ואז פעלתי על סמך האימות הזה עד 17:53 — בתוך החלון הזה נפתח סשן חדש, `auto-routing-bf`, שקימט את `b266d88` **שישים ותשע שניות** לפני הקומיט שלי. אימות אינו רישיון לסשן: הוא צילום רגע, והחלון שבו הוא תקף נסגר ברגע שמישהו יכול להיפתח. גרוע מכך, קימטתי עם `git add -A` — בעץ משותף זו פקודה שסורקת את **כל** ספריית העבודה, כולל עבודה בתנועה של סשן אחר, ומקמטת אותה בשמך. הפעם היא תפסה רק את שלושת הקבצים שלי, אבל זה היה מזל ולא זהירות. שני כללים: (א) אמת מחדש (`git status`, `git log`, `ListAgents`) מיד לפני כל מקבץ כתיבה, לא פעם אחת בתחילת הסשן; (ב) בעץ שעלול להיות משותף — מנה את הקבצים במפורש (`git add <path> <path>`), לעולם לא `-A` ולא `git commit -a`. הסכנה גדלה באותו יום ממש: `b266d88` שכתב מחדש את כלל ה-`.scratch` ב-`.gitignore` (מ-`.scratch/` ל-`.scratch/**` עם החרגות-נגד), והפך שישים קובצי טיקט/מפה/מפרט מבלתי-נראים ל**מנוהלים** — כלומר ל-`git add -A` יש עכשיו שישים קבצים נוספים לסחוף שקודם היו בטוחים. הסשן העמית עצמו קימט עם pathspec מפורש בדיוק כי המחיקה שלי ישבה ב-index באותו רגע. הרחבה מעשית של "שני כותבים על עץ עבודה אחד" למטה.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **מחיקת ענף מוזג בטוחה לקוד ושקטה-שגויה לכל מסמך שמזכיר אותו:** מחקתי את `spec/0004-learning-loop` אחרי שאימתתי שהוא מוזג במלואו — נכון מבחינת קוד, אפס אובדן. אבל שורת הסטטוס של טיקט 12 הצביעה על אותו ענף, וההצבעה הפכה בשקט למתה; סשן עמית תיקן אותה ל-`main` ב-`b266d88`. בנוסף, המחיקה נראתה מבחוץ כהיעלמות בלתי-מוסברת: העמית מנה את הענף ואת ה-worktree דקות קודם, ובקריאה הבאה שניהם נעלמו בלי הסבר זמין מתוך הסשן שלו. הכלל: `git branch --merged` עונה רק על "האם הקוד בטוח". לפני מחיקת ענף, הרץ גם `grep -rn "<branch-name>"` על התיעוד והטיקטים, ואם עוד סשן פעיל — הודע לו, כי היעלמות ללא הסבר עולה לו בזמן אמיתי.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **`ps` משקר לשני הכיוונים כשמאמתים "כותב יחיד":** רשימת התהליכים הראתה שני `codex` שנשאו `[WORKER-MODE: AGY-NESTED-EXEC]` בתוך ה-prompt — חתימה זהה לפרוטוקול של הריפו הזה — ורק `lsof -a -p <PID> -d cwd` הוכיח שהם יושבים ב-`/Users/liorparente/Projects/Yamit-Therapy`, ריפו אחר לגמרי (חיובי-שגוי). במקביל, הסשן שכן כתב לעץ הזה לא הופיע כתהליך נבדל בכלל, ורק `ListAgents` חשף אותו (שלילי-שגוי). כלל "כותב יחיד" מקבל כאן נוהל אימות בן שלושה מכשירים בלתי-תלויים: `cwd` מכריע על תהליכים, `ListAgents` על סשני עמית, ושינוי מצב בין שתי קריאות קריאה-בלבד שלך עצמך (קובץ שעבר מ-unstaged לקומיט באמצע החקירה) הוא ההוכחה החזקה מכולן. אף אחד מהם אינו מספיק לבדו.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **שתיקת עמית אינה הסכמה, והמתנה לתשובתו עלולה לתקוע לנצח:** שלחתי הודעת תיאום ל-`auto-routing-e2` ושאלתי אם הוא עדיין כותב לעץ; הוא נסגר לפני שעיבד אותה ולעולם לא ענה. ההיתר להתקדם הגיע מהיעלמותו מ-`ListAgents` בתוספת בדיקות עצמאיות על העץ (נקי, ללא `index.lock`, ללא קומיטים או שינויי קבצים חדשים) — לא מהסכמתו. תיאום עם סוכן עמית הוא בקשה, לא לחיצת-יד: קבע מראש מה יהווה הכרעה בהיעדר תשובה, אחרת יצרת נעילה הדדית שבה שני הצדדים ממתינים.
- `[2026-08-13] [auto-routing] [importance:4] [pattern]` - **תצוגה מקדימה של מיזוג ללא תופעות לוואי:** `git merge-tree --write-tree <A> <B>` מכריע קונפליקטים בלי לגעת ב-index, בעץ העבודה או ב-`HEAD` — יציאה אפס והחזרת גיבוב עץ פירושה מיזוג נקי. בשילוב `git diff --name-only <merge-base> <side>` על שני הצדדים, קבוצות קבצים זרות מסבירות *מדוע* הוא נקי ולא רק *ש*הוא נקי. כך עונים על "האם זה יתנגש?" עוד לפני שלוקחים את תפקיד הכותב — בדיוק מה שנדרש כשכלל כותב-יחיד אוסר להתחיל מיזוג ספקולטיבי ואז לבטלו.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **"אני לא מזהה את זה" אינו ראיה על הקוד אלא על חלון ההקשר שלי:** קבעתי — פעמיים, ואף רשמתי ל-`ERRORS.md` בקומיט `b5c750a` — ש-`implementation_plan.md` בשורש "שייך לפרויקט אחר לגמרי", כי הוא מדבר על `AuditIssue`, `WARN-01`/`WARN-02` ו-`discovery_ordinal` "שאינם קיימים בקוד הזה". כולם קיימים: [`routing_check.py:164`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/routing_check.py:164) מגדיר `AuditIssue` עם `discovery_ordinal`, ו-[`routing_check.py:940`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/routing_check.py:940) מיישם בדיוק את קדימות `WARN-01` על `WARN-02`. הקובץ היה התוכנית של הריפו הזה עצמו מ-2026-08-05/06 (`2fc92d5` → `27058f8` → `7ac1940`), והעבודה שתיארה יושמה. הסקתי מקור מתוך חוסר-היכרות, והחלפתי "נעדר מהחלק שקראתי" ב"נעדר מהקוד", בלי להריץ את ה-`grep` היחיד שהיה מכריע. לפני שמייחסים קובץ לפרויקט אחר — `grep` למזהים הייחודיים שבו, לא רק לנתיב שלו.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **נתיב תוצר-התוכנית נושא משקל, ולכן שריד עליו הוא קלט חי:** [`advisory_consultation.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/advisory_consultation.py) קובע `plan_path = root_dir / "implementation_plan.md"`, כותב אליו, ומוחק אותו דרך `_remove_stale_plan_artifact` — שנועדה במפורש לנקות "תוכנית קיימת מריצה קודמת", ושכבר גרמה לבאג מחיקת-הקנרית שתוקן ב-`aa118f1`. קובץ ישן שיושב שם אינו ארטיפקט מת אלא תצורה מיושנת, וזה גרוע מהיעדר תצורה. הכלל: לפני שפוטרים קובץ תלוש כ"שארית לא מזיקה", `grep` לנתיב שלו בקוד — אם משהו קורא או מוחק אותו, הוא תצורה. הקובץ נמחק ב-2026-08-13 לבקשת המשתמש וניתן לשחזור דרך `git show 7ac1940:implementation_plan.md`. קשור ל"מסלול חסימה מוקדמת שמחק תוכנית אמיתית" למטה.
- `[2026-08-13] [auto-routing] [importance:3] [pattern]` - **`-d` הקטן הופך את בדיקת הבטיחות למובנית:** `git branch -d` מסרב למחוק ענף שלא מוזג, כך שהאימות שכבר ביצעת ידנית נאכף שוב על ידי הכלי עצמו ברגע ההרצה; `-D` הגדול מוותר בדיוק על ההגנה הזאת. אותה צורה כמו הכלל המתועד ל-worktree (`git branch --contains <sha>` לפני הסרה), ולפניו כדאי גם `git status --porcelain --ignored` כדי לראות מה בדיוק ייעלם — כאן רק מטמוני `.mypy_cache`/`.ruff_cache`/`__pycache__`. העדף תמיד את הווריאנט שמסרב על הווריאנט שמציית.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **שני כותבים על עץ עבודה אחד:** שני סשני Claude Code כתבו במקביל ל-`advisory_consultation.py`/`test_routing.py` על אותו ענף `main` ב-2026-08-12 — התנגשות שהולידה בלגן אמיתי ואת כלל "כותב יחיד, רציף" לכל שאר הסשן הזה (עץ עבודה אחד לכל סשן, לא מקבילי). ב-2026-08-13 גם נדרש ניקוי `git worktree remove` על עותק ישן (`review-snapshot`, HEAD מנותק) שנוצר מאותה תקופה, אחרי שווידאתי שהקומיט שלו (`23a138c`) כבר נמצא בהיסטוריית הענף האמיתי ואינו עתיד לאבוד.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **קובץ בדיקות שנבדק ולעולם לא רץ:** `ruff`/`mypy` יכולים לבדוק קובץ `test_*.py` שאף שלב ב-CI לא מריץ בפועל — כך שהבדיקות שבתוכו "ירוקות" רק כי הן מעולם לא בוצעו. קרה פעם אחת ל-[`test_production_invoker.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/test_production_invoker.py) (שש-עשרה בדיקות), ונחסם מפני הישנות בעזרת בדיקה ייעודית ב-`test_routing.py` (`test_ci_runs_every_test_file_it_checks`) שמאמתת שכל קובץ שנבדק גם רץ. ב-2026-08-13 נאלצתי להוסיף לאותה בדיקה חריג צר ומתועד בשם (`_CHECKED_BUT_NOT_EXECUTED_BY_DESIGN`) עבור [`test_lmstudio.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/test_lmstudio.py) — קובץ שמחייב שרת LM Studio חי ולכן *במכוון* אינו רץ ב-CI. "נבדק" אינו "רץ", אלא אם משהו אוכף את זה כבדיקה.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **מסלול חסימה מוקדמת שמחק תוכנית אמיתית:** דרגה 3 של סולם דעיכת התקציב חוזרת לפני שנוגעים בעובד כלשהו, וקריאה בלתי-מותנית ל-`_remove_stale_plan_artifact` באותו מסלול מחקה את `implementation_plan.md` של משימה אמיתית גם כשה-run היה בדיקת קנרית (`is_canary=True`) — זיהוי סינתטי, לא משימה. תוקן ב-`aa118f1`. הלקח: מסלול חסימה-מוקדמת (early return / preemption) אסור שיניח ש"תוצאה של היום" חלה גם על ריצת בדיקה — יש למפות כל תופעת-לוואי (מחיקת קובץ, כתיבה) שהמסלול המקוצר מבצע בנפרד מהתוצאה הרגילה.
- `[2026-08-13] [auto-routing] [importance:4] [pattern]` - **הרחבת שער חוסם-כשל דרך "האם ניתן לדעת", לא רק "האם זוהה":** כדי להוסיף יכולת חדשה (דיאלוג מקומי-בלבד למשימה רגישה) בלי לגעת בהתנהגות הקיימת והבדוקה, השער ב-[`advisory_consultation.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/advisory_consultation.py) הסתעף על `marker is not None and reachability_check is None` — לא רק על זיהוי הסימן. כשאין דרך לדעת (`reachability_check` לא סופק), ההתנהגות הישנה נשמרת בדיוק; רק כשיש דרך לדעת, נפתח המסלול החדש. כלל כללי: בהרחבת שער חוסם-כשל בהתנהגות חדשה, יש להסתעף על "האם יש יכולת להכריע" ולא רק על תנאי ההפעלה המקורי.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **תוואי חדש בפונקציית ליבה לא מגיע אוטומטית למפיץ במורד הזרם:** ל-`dispatch_post_mortem_consultation` יש רשימת פרמטרים משלה, נפרדת מזו של `run_advisory_consultation_debate` — הוספת מושב חדש (`reachability_check`) לפונקציית הליבה לא חשפה אותו במפיץ עד שתוקן במפורש. נמצא רק כי ציר הסקירה של המפרט קרא את מסלול ה-dispatch באופן ספציפי, לא רק בדק את פונקציית הליבה. כלל: כשמשחילים מושב חדש דרך מערכת עם כמה נקודות כניסה, יש למנות כל נקודת כניסה בנפרד ולא להניח שתוואי בליבה מתפשט אוטומטית.
- `[2026-08-13] [auto-routing] [importance:5] [gotcha]` - **שני אוצרות מילים מוכרזים בנפרד סוחפים בשקט:** `Occasion` ([`advisory_consultation.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/advisory_consultation.py), עם מקפים — `"plan-review"`) ו-`DialogueOccasion` ([`learning_journal.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/learning_journal.py), עם קווים תחתונים — `"plan_review"`) אמורים לתאר את אותו אוצר מילים, וסחפו על שלושה מארבעה ערכים בלי שאף בדיקה תפסה — כי כל צד בנה את הבדיקות שלו באופן עצמאי-ועקבי-עם-עצמו, ואף בדיקה לא בנתה אובייקט מהצד האחד מתוך ערך אמיתי מהצד השני. מערכת הטיפוסים של Python אינה אוכפת התאמה בין שני כינויי `Literal` שהוכרזו בנפרד. כשמודולים/מפרטים שונים אמורים לחלוק אוצר מילים — יש לנעול את ההתאמה בבדיקה מפורשת (`set(get_args(A)) == set(get_args(B))`), לא לסמוך על שני כותבים שיצדקו במקביל.
- `[2026-08-13] [auto-routing] [importance:4] [gotcha]` - **מסמך משימה שמתאר עבודה עתידית כעובדה קיימת:** בריף המיזוג טען ש"היומן מסנן רשומות קנרית לפני אגרגציה" בלשון הווה, כשבפועל הקוד ב-`learning_journal.py` הוא סכימה בלבד ללא כותב — אין קוד שקורא את `.ralph/routing_telemetry.jsonl` בכלל. לפני שמסמנים סעיף בדיקה כ"בוצע", יש לוודא קודם שההתנהגות המתוארת קיימת בכלל בקוד, ולא להסיק מניסוח הסעיף שהיא חיה.

- `[2026-08-11] [lmstudio-test-suite] [importance:4] [pattern]` - **סוויטת בדיקות מקצה לקצה ב-Python מול LM Studio API:** קובץ הבדיקות [`test_lmstudio.py`](file:///Users/liorparente/Projects/auto-routing/skills/worker-routing/test_lmstudio.py) מיישם 4 בדיקות יחידה מרכזיות מול השרת המקומי בפורט 1234: Health check (`READY`), ג׳נרוט פונקציית פייתון (`is_prime`), היצמדות ל-System Prompt, והזרמת SSE Streaming.
- `[2026-08-11] [auto-routing] [importance:5] [architecture]` - **נקודת מפגש מבטיחה רק את מה שמגיע אליה:** ניתוב כל דרכי היציאה של פונקציה דרך סוגר אחד (כמו `_result` ב-`advisory_consultation.py`) נראה כערובה מבנית לכך שכל יציאה כותבת את תוצריה — אך הוא מכסה אך ורק קוד שמגיע לסוגר. ב-`run_advisory_consultation_debate` כתיבת קובץ התוכנית בנתיב ההסכמה קרתה *לפני* הסוגר וללא הגנה, כך ש-`OSError` עף החוצה ודווקא היציאה היחידה שהגיעה להסכמה איבדה את שלושת תוצריה. כשמכריזים על נקודת מפגש כערובה, חובה למפות אילו פעולות קלט-פלט מתרחשות לפניה.
- `[2026-08-11] [auto-routing] [importance:5] [gotcha]` - **גיבוב אינו הסתרה (Confirmation Oracle):** תקציר גיבוב מקוצר (למשל `sha256[:16]`) על טקסט שניתן לנחש אינו מזהה "לא חושפני" — הוא כלי המאפשר לאמת ניחוש. כלל מתועד שאומר "שום דבר ה*נגזר* מתוך איקס לא יוצא החוצה" מתכוון לנגזר, ולא רק למופע המילולי; בדיקה שמאמתת היעדר מחרוזת בלבד לא תתפוס הפרה כזאת מבחינה מבנית. במסלול סירוב ביטחוני יש להשתמש בערך שאינו נגזר כלל מטקסט המשימה.
- `[2026-08-11] [auto-routing] [importance:4] [workflow]` - **מחלוקת בין שני צירי סקירה היא אות, לא רעש:** בסקירה הדו-צירית ציר התקנים קבע "הפרה חמורה" וציר המפרט קבע "הגבול מחזיק" על אותו קוד בדיוק. שניהם דייקו — ציר המפרט בדק אם הסוד *מופיע* בתוצרים, וציר התקנים בדק אם משהו ה*נגזר* ממנו דולף. הפרדת הצירים היא מה שחשף את הפער, ומיזוג או דירוג מחדש של הממצאים היה מסתיר אותו. כשהצירים חלוקים — יש לבדוק בעצמך איזו שאלה כל אחד שאל, לא להכריע ביניהם.
- `[2026-08-11] [auto-routing] [importance:4] [workflow]` - **אין לסקור הפרש בזמן שעובד עדיין רץ:** הפעלת סוכני סקירה על הפרש שעובד עדיין משנה מבזבזת את שניהם — בסשן זה קובץ המבחנים היה ריק משינויים בעת הבקשה, וחמישה מששת תנאי הקבלה היו תנאי מבחן. לפני הפעלת סקירה: לוודא שתהליך העובד הסתיים, ושקובצי המבחן אכן מופיעים ב-`git status`.
- `[2026-08-11] [auto-routing] [importance:4] [gotcha]` - **דיווח עובד על קבצים שאינם בבעלותו אינו אימות:** עובד ה-CLI הצהיר שארבעת קובצי התיעוד "לא השתנו מאז תחילת הסשן", בעוד ש-`knowledge/institutional-memory.md` גדל בפועל באותו חלון זמן מסשן מקביל. הצהרה של עובד על קובץ שהוא עצמו לא נגע בו היא ניחוש ולא ממצא. יש לאמת מצב קבצים ב-`git status` / `git diff` באופן עצמאי, בדיוק כפי שמאמתים את שערי הבדיקות.
- `[2026-08-11] [auto-routing] [importance:5] [gotcha]` - **מניעת התנגשויות בין סוכנים בסשנים מקבילים:** כאשר סוכן נוסף עובד במקביל בסשן נפרד על משימה (כמו טיקט 06), אסור לסמן את הטיקט כ-`completed` או לבצע `git commit` מאחורי גבו. יש לבדוק קודם את ה-diff הנמצא בתהליך עבודה מול המשתמש.
- `[2026-08-11] [auto-routing] [importance:4] [workflow]` - **הרצת install.sh ופקודות Git מחוץ לסנדבוקס:** הרצת `./install.sh .` או `git worktree remove` מתוך ה-IDE דורשת הרצה במישור `BypassSandbox: true`, כיוון שסקריפט ההתקנה מעדכן מניפסטים ב-`~/.gemini/` וקובצי מערכת מחוץ לגבולות ה-Workspace המקומי.
- `[2026-08-11] [auto-routing] [importance:4] [gotcha]` - **זיהוי ואיפוס Git Worktrees של Claude Code:** Claude Code מייצר Worktrees נפרדים תחת `.claude/worktrees/`. כדי למנוע פערים בקריאת סטטוס הטיקטים בין העוזרים, יש לבדוק ב-`git worktree list` ולבצע ניקוי במידת הצורך לאחר סיום המשימה.
- `[2026-08-11] [auto-routing] [importance:3] [workflow]` - **אימות מרובע של סטטוס טיקטים:** לפני קביעה שטיקט מסוים פתוח או סגור, יש לבצע הצלבה בין קובץ ה-Issue, ה-`git log`, ה-diff הלא-committed בענף הנוכחי, וסשנים פעילים נוספים.

- `[2026-08-11] [auto-routing] [importance:4] [gotcha]` - **עדכון סטטוס מפרטים ב-Git (`docs/specs/`):** קבצי מפרט (`docs/specs/`) שנשארים בסטטוס `Ready for agent` עלולים ליצור חיווי שווא של משימות פתוחות, אפילו כשכל הטיקטים מומשו ונבדקו ב-Git. חובה לעדכן את סטטוס המפרט או לסגור אותו עם גמר היישום.


- `[2026-08-06] [auto-routing] [importance:5] [pattern]` - **תאימות לאחור ב-Python (API Break):** החלפת פרמטר מיקומי ראשון (Positional Argument) בבנאי של תשתית (כמו `RoutingAuditEngine`) שוברת קריאות קיימות. בעת שדרוג בנאים, חובה להשתמש ב-`*` כדי לאלץ שימוש ב-Keyword-only arguments עבור פרמטרים חדשים או לשמור על הסדר המקורי.
- `[2026-08-06] [auto-routing] [importance:5] [gotcha]` - **עקיפת מנגנון אבטחה (DEC-01):** יציאה מוקדמת מהקוד עבור כלים ללא דגלים (כמו `agy`) עקפה בטעות גם את אימות הזהות של הסוכן. בבדיקות ניתוב ואבטחה, יש להקפיד להפריד לחלוטין בין דילוג על בדיקת פרמטרים אופציונליים לבין בדיקת הזהות הקריטית שחייבת להתקיים תמיד.
- `[2026-08-06] [auto-routing] [importance:4] [gotcha]` - **סקירת קוד (Codex Review) מול Working Tree מלוכלך:** פקודת סקירת הקוד רצה גם על שינויים שלא במעקב (Untracked Files, כמו Golden Fixtures) או שרידים מקומיטים שבוטלו. חובה לוודא שסביבת העבודה מסודרת ונקייה לפני הרצת סקירה כדי למנוע חיובי-שווא (False positives).
- `[2026-08-08] [auto-routing] [importance:5] [gotcha]` - **macOS Sandbox & CLI Socket Binding (אימות אמפירי):** בסביבת IDE Sandbox ב-macOS, הרצת כלי CLI של מודלים (כמו `codex exec` או `claude -p`) מובילה לשגיאת הרשאות `bind: 127.0.0.1:0` (`Operation not permitted`). משתני סביבה כמו `TMPDIR=/tmp` או `GIT_OPTIONAL_LOCKS=0` אינם פותרים זאת. הפתרון הבלעדי והמחייב הינו הרצת ה-Tool עם `BypassSandbox: true` כפי שמוגדר בסעיף 4.7 ב-`protocol.md`.
- `[2026-08-08] [auto-routing] [importance:4] [pattern]` - **סנון חיובי-שווא בסקירת קוד (Code Review vs ADRs):** סוכני סקירת קוד שאינם מודעים להקשר השיחה עשויים לסמן מנגנוני תאימות לאחור (כמו Deprecated Facades) כריחות קוד. חובה להצליב ממצאי ביקורת מול ה-ADRs והחלטות ה-Grilling כדי לסנן אזהרות שווא.
- `[2026-08-08] [auto-routing] [importance:4] [pattern]` - **הפרדת ניתוח צעד בודד (`_analyze_step` & `StepAnalysis`):** ריכוז לוגיקת המדיניות בתוך פונקציה טהורה ומחזירת אובייקט `StepAnalysis` בלתי-משתנה מפשט את מנוע הביקורת ומאפשר בדיקות יחידה מבודדות ללא דליפת מצב.
